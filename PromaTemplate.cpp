#include<bits/stdc++.h>

#define ll long long
#define debug(x) cout<< #x<< ": "<< x<< "\n"
#define case(x, y) cout<<"Case " << x<<": "<< y<< "\n" 
#define vint vector<int>
#define vll vector<ll>
#define pin pair<int, int>

using namespace std;

void debugar(int *an, int n)
{
    for(int i=0; i<n; i++)
    {
        cout<< an[i]<< " ";
    }
    cout<< "\n";
}

void debugllar(ll *an, int n)
{
    for(int i=0; i<n; i++)
    {
        cout<< an[i]<< " ";
    }
    cout<< "\n";
}

void solve(int cnt)
{
    
}

int main()
{
    int t=1, cnt=0;
    cin>> t;
    while(t--)
    {
        solve(++cnt);
    }
}